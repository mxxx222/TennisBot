# Core HTTP and Async Libraries
aiohttp==3.9.1
requests==2.31.0

# Web Scraping
selenium==4.15.2  # Consider migrating to Playwright for 40-50% better performance on Apple Silicon
beautifulsoup4==4.12.2
lxml==4.9.3  # Apple Silicon optimized: 3x faster HTML parsing than BeautifulSoup

# Browser Automation (Apple Silicon optimized alternatives)
playwright==1.56.0  # 40-50% faster scraping on M4, no-hang reliability
undetected-chromedriver==3.5.4
webdriver-manager==4.0.1
fake-useragent==1.4.0

# Data Processing (Apple Silicon optimized - uses Accelerate framework)
pandas==2.1.4  # ARM64 native build recommended via Homebrew: brew install pandas
numpy==1.26.2  # ARM64 native build - ensure Accelerate framework is used
openpyxl==3.1.2  # For reading Excel files (.xlsx)
xlrd==2.0.1  # For reading legacy Excel files (.xls)

# Machine Learning (for ITF match predictor) - ARM64 optimized
scikit-learn==1.3.2  # ARM64 native build
joblib==1.3.2  # Uses multiprocessing efficiently on Apple Silicon
xgboost==2.0.3  # ARM64 native build - verify with: python -c "import xgboost; print(xgboost.__version__)"
lightgbm==4.1.0  # ARM64 native build - verify installation

# Configuration and Environment
PyYAML==6.0.1
python-dotenv==1.0.0

# Logging
loguru==0.7.2

# Error Tracking and Monitoring
sentry-sdk==1.38.0


# Utilities
click==8.1.7
tqdm==4.66.1
colorama==0.4.6
schedule==1.2.0

# OpenAI Integration (for AI analyzer)
openai==1.3.7
tiktoken==0.5.2

# Notion Integration
notion-client==2.2.1

# Telegram Bot (for notifications)
python-telegram-bot==20.7
pytz==2023.3

# Development and Testing (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
